[project]
name = "backend"
version = "0.1.0"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "sqlalchemy",
    "asyncpg"   # if you're using PostgreSQL async
]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"